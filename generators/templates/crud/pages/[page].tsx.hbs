import { {{pascalCase name}}ListTablePage } from "screens/{{camelCase name}}/list/table";
import { GetServerSideProps } from "next";
import { withSSRAuth } from "shared/libs/utils";
import { get{{pascalCase name}}s } from "entidades/{{camelCase name}}/{{camelCase name}}.api";
export const getServerSideProps: GetServerSideProps = withSSRAuth(async (context) => {
  const page = Number(context?.query?.page ?? 1);
  const data = await get{{pascalCase name}}s(page, context);
  return {
    props: {
      data,
      page,
    },
  };
});

export default {{pascalCase name}}ListTablePage;
