export type {{pascalCase name}}Props = {
  _id: string;
  name: string;
  createdAt: string;
  value?: boolean;
  active?: boolean;
};

class {{pascalCase name}} {
  protected props: {{pascalCase name}}Props;
  constructor(props: {{pascalCase name}}Props) {
    this.props = props;
  }
  public static build(props: {{pascalCase name}}Props) {
    return new {{pascalCase name}}(props);
  }
  get _id(): string {
    return this.props._id;
  }
  get name(): string {
    return this.props.name;
  }
  get createdAt(): string {
    return this.props.createdAt;
  }
  get active(): boolean | undefined {
    return this.props.active;
  }
  format(): {{pascalCase name}}Props {
    return {
      ...this.props,
      _id: this.props._id,
      name: this.props.name,
      active: this.props.active,
      value: false,
      createdAt: new Date(this.props.createdAt).toLocaleDateString("pt-BR", {
        day: "2-digit",
        month: "2-digit",
        year: "numeric",
      }),
    };
  }
}
export const {{camelCase name}}Model = (props: {{pascalCase name}}Props) => {{pascalCase name}}.build(props);
