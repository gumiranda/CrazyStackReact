import { {{pascalCase name}} as {{pascalCase name}}Chakra, memo } from "@chakra-ui/react";
export const {{pascalCase name}}_ = ({ children, datatestid, ...rest }: any, ref) => {
  return (
    <{{pascalCase name}}Chakra ref={ref} {...rest} data-testid={datatestid ?? "{{pascalCase name}}TestId"}>
      {children}
    </{{pascalCase name}}Chakra>
  );
};
export const {{pascalCase name}} = memo({{pascalCase name}}_);
