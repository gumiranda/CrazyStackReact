import { config } from "@/application/config";
import { Layout } from "@/shared/ui/templates/Layout";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: `${config.systemName} | {{pascalCase label}}s`,
  description: `Página de listagem de {{camelCase label}}s do ${config.systemName}. Aqui você pode criar, editar e excluir {{camelCase label}}s.`,
};

export default function LayoutAuthenticated({ children }) {
  return <Layout>{children}</Layout>;
}
