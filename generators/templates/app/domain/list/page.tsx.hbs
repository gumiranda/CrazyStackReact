import { config } from "@/application/config";
import { {{pascalCase name}}GridPage } from "@/slices/appointments/screens/{{camelCase name}}/list/grid";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: `${config.systemName} | {{pascalCase label}}s`,
  description: `Página de listagem de {{camelCase label}}s do ${config.systemName}. Aqui você pode ver as {{camelCase label}}s em lista infinita.`,
};
export default {{pascalCase name}}GridPage;
